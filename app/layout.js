import "./globals.css";

export const metadata = {
  title: "Disciples — Convert the AI",
  description: "How fast can you convert the AI?",
};

export default function RootLayout({ children }) {
  return <html lang="en"><body>{children}</body></html>;
}
